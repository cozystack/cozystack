---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: cozystackplatforms.cozystack.io
spec:
  group: cozystack.io
  names:
    kind: CozystackPlatform
    listKind: CozystackPlatformList
    plural: cozystackplatforms
    singular: cozystackplatform
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CozystackPlatform is the Schema for the cozystackplatforms API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CozystackPlatformSpec defines the desired state of CozystackPlatform
            properties:
              basePath:
                description: |-
                  BasePath is the base path where the platform chart is located in the source.
                  For GitRepository, defaults to "packages/core/platform" if not specified.
                  For OCIRepository, defaults to "core/platform" if not specified.
                type: string
              interval:
                default: 5m
                description: Interval is the interval at which to reconcile the HelmRelease
                type: string
              sourceRef:
                description: |-
                  SourceRef is the source reference for the platform chart
                  This is used to generate the ArtifactGenerator
                properties:
                  kind:
                    default: HelmRepository
                    description: Kind of the source reference
                    type: string
                  name:
                    description: Name of the source reference
                    type: string
                  namespace:
                    default: cozy-public
                    description: Namespace of the source reference
                    type: string
                required:
                - kind
                - name
                - namespace
                type: object
              values:
                description: |-
                  Values contains Helm chart values as a JSON object
                  These values are passed directly to HelmRelease.values
                x-kubernetes-preserve-unknown-fields: true
            required:
            - sourceRef
            type: object
        type: object
    served: true
    storage: true
