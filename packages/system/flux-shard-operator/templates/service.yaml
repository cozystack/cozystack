apiVersion: v1
kind: Service
metadata:
  name: flux-shard-operator
  labels:
    app: flux-shard-operator
spec:
  trafficDistribution: PreferClose
  type: ClusterIP
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: webhook
  selector:
    app: flux-shard-operator
