{{- /*
  tracingStorages is empty by default, so tracing is off out of the box and no
  VTCluster/VTSingle is provisioned. Unlike logsStorages, an absent backend
  drops no data here: nothing produces spans until the trace collector ships, so
  there is no loud-failure guard yet (the collector likewise renders only once a
  backend is configured).

  Shared-central topology (tracingCollector.backend: central): this tenant has
  no local backend — the collector exports to the shared VictoriaTraces in
  tenant-root — so no VTCluster/VTSingle is rendered here either.

  The readiness-gate healthCheckExprs on VTCluster/VTSingle stay unconditional:
  Flux evaluates them only against CRs present in the release inventory, so with
  nothing rendered they are inert.
*/ -}}
{{- if ne (.Values.tracingCollector.backend | default "local") "central" }}
{{- range .Values.tracingStorages }}
{{- $mode := .mode | default "cluster" }}
{{- if and (ne $mode "cluster") (ne $mode "single") }}
{{- fail (printf "monitoring: tracingStorages[%s].mode must be either \"cluster\" or \"single\"" .name) }}
{{- end }}
---
{{- if eq $mode "single" }}
apiVersion: operator.victoriametrics.com/v1
kind: VTSingle
metadata:
  name: {{ .name }}
spec:
  managedMetadata:
    labels:
      apps.cozystack.io/application.group: apps.cozystack.io
      apps.cozystack.io/application.kind: Monitoring
      apps.cozystack.io/application.name: {{ $.Release.Name }}
  retentionPeriod: {{ .retentionPeriod | quote }}
  {{- with .retentionDiskUsageBytes }}
  retentionMaxDiskSpaceUsageBytes: {{ . | quote }}
  {{- end }}
  # Stamped on the storage PVC so the monitoring post-delete cleanup hook can
  # select it by this label (same release-scoped cleanup as logs/metrics).
  storageMetadata:
    labels:
      apps.cozystack.io/application.name: {{ $.Release.Name }}
  storage:
    {{- with .storageClassName }}
    storageClassName: {{ . }}
    {{- end }}
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: {{ .storage }}
{{- else }}
apiVersion: operator.victoriametrics.com/v1
kind: VTCluster
metadata:
  name: {{ .name }}
spec:
  managedMetadata:
    labels:
      apps.cozystack.io/application.group: apps.cozystack.io
      apps.cozystack.io/application.kind: Monitoring
      apps.cozystack.io/application.name: {{ $.Release.Name }}
  insert:
    replicaCount: 2
    resources: {}
  select:
    replicaCount: 2
    resources: {}
  storage:
    retentionPeriod: {{ .retentionPeriod | quote }}
    {{- with .retentionDiskUsageBytes }}
    retentionMaxDiskSpaceUsageBytes: {{ . | quote }}
    {{- end }}
    replicaCount: 2
    resources: {}
    storage:
      volumeClaimTemplate:
        metadata:
          # Stamped on the claim template so vm-operator copies it onto the
          # storage PVCs; managedMetadata is not applied to PVCs. The monitoring
          # post-delete cleanup hook selects PVCs by this label to stay
          # release-scoped (mirrors vlogs/vmcluster).
          labels:
            apps.cozystack.io/application.name: {{ $.Release.Name }}
        spec:
          {{- with .storageClassName }}
          storageClassName: {{ . }}
          {{- end }}
          resources:
            requests:
              storage: {{ .storage }}
{{- end }}
{{- end }}
{{- end }}
