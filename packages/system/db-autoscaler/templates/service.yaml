apiVersion: v1
kind: Service
metadata:
  name: db-autoscaler
  labels:
    app: db-autoscaler
spec:
  trafficDistribution: PreferClose
  type: ClusterIP
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: webhook
  selector:
    app: db-autoscaler
